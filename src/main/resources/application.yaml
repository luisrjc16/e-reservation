spring:
  datasource:    url: jdbc:mysql://localhost:3306/test #jdbc:postgresql://localhost_5432/postgres
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
      generate-ddl: true
    show-sql: true